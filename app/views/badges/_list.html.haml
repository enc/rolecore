%ul
  - @badges.each do |item|
    %li
      - if item.score <= 3
        %span.label.warning Warnung
      - else
        %span.label.important Achtung
      = item.message.description
      = item.comment
  - if @badges.empty?
    %li
      Keine Hinweise!
