
.container-fluid
  .sidebar

    %h1 Listing users

    %table
      %tr
        %th Name

      - @users.each do |user|
        %tr
          %td= link_to user.name, user, remote: true

    %br

    = link_to 'New User', new_user_path

  #user.content.userbox
    %h2 Userbox
