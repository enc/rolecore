= form_for @relation_property do |f|
  -if @relation_property.errors.any?
    #error_explanation
      %h2= "#{pluralize(@relation_property.errors.count, "error")} prohibited this relation_property from being saved:"
      %ul
        - @relation_property.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :relation_id
    = f.number_field :relation_id
  .actions
    = f.submit 'Save'
